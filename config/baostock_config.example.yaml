# BaoStock 同步至 MySQL（复制为 baostock_config.yaml 后修改）
#
# 运行: python scripts/sync_baostock_daily.py
# 密码建议用环境变量: BLADER_DB_USER / BLADER_DB_PASSWORD
# 若未创建本文件，将自动回退读取 config/ifind_config.yaml 的 database 段

database:
  host: 117.25.133.3
  port: 3306
  name: blader
  username: stock-dev
  password: ""
  # url: "mysql+pymysql://user:pass@host:3306/blader?charset=utf8mb4"

sync:
  years: 3.0
  daily_api_limit: 40000
  use_full_universe: true

tables:
  daily: stock_daily_qfq
  quota: api_quota
  sync_state: stock_sync_state

# 前复权 adjustflag=2；代码存 THS 格式 600000.SH
